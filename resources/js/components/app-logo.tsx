import { MonLogo } from '@/components/mon-logo';

export default function AppLogo() {
    return (
        <MonLogo className="h-12 w-auto max-w-[220px] shrink-0 group-data-[collapsible=icon]:h-8 group-data-[collapsible=icon]:max-w-[2rem]" />
    );
}
